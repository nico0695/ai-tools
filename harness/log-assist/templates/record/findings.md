# Findings

<!-- Owner: loga-analyze, worker. One entry per observation that is actually
     supported by evidence. An observation without a resolving citation is not
     a finding. -->

## Digest

- standing:
- discarded:
- last round:
- blocker:

## Findings

### F-01 [R1] <one line: what was observed>

- screen: `<id>` · source: `script (loga_<name>)` | `exploratory` | `user`
- evidence: `logs/<file>:<line>` — "<verbatim excerpt, up to ~200 chars>"
- confidence: `high | medium | low`

## Discarded

<!-- One line each: id, round transition, reason, citation. This is what the
     "ruled out" section of a report is built from. -->

- F-xx [R1→R2] <why it no longer stands> — `logs/<file>:<line>`
