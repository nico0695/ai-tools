# <title> — <ref>

<!-- Owner: the orchestrator, rewritten after every step. About one screen.
     Standing entries only: discarded material lives in record/, not here.
     Headings stay in English; prose follows `language` in loga.config.toml. -->

| | |
|---|---|
| Status | `<status>` · outcome `<outcome or —>` · round `<n>` |
| Scope | `<platform>` · player `<version>` · screens `<list>` |
| Window | `<incident_window or —>` |
| Jira | `<key or —>` |

## Problem

<Two to four lines: what was reported, and what was expected instead.>

## Findings

- **F-xx** <one line> — `logs/<file>:<line>`

## Hypotheses

- **H-xx** <one line> · `<open | supported | refuted>` · would confirm: <what>

## Discarded

- **F-xx** <one line, why it was ruled out> — `logs/<file>:<line>`

## Next

- <next_action>
- Open questions: <list, or —>

## Reports

- `reports/<YYYYMMDD>-<findings|status>.md`
